# Secrets — never commit
.env

# Personal dogfood configs (personal vault paths, domains — not part of open-core template)
config/domains.*.yaml
!config/domains.yaml

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Runtime artifacts
logs/
*.log
data/

# OS / editor
.DS_Store
*.bak
*.swp

# Agent team workflow — private dev infra, never publish
.claude/
CLAUDE.md

# Agent team work artifacts — internal recon/drafts, never publish
digests/
drafts/
stories/
specs/
research-briefs/
validations/
